nelmio_api_doc:
    models: { use_jms: false }
    documentation:
        info:
            title: MHCC
            description: Coding challenge example app!
            version: 1.0.0
    areas: # to filter documented areas
        path_patterns:
            - ^/api(?!/doc.json$) # Accepts routes under /api except /api/doc